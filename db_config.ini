# Database Configuration
# Update these values with your MySQL database credentials

[database]
host = "localhost"
user = "root"
password = "root"
database = "welvision_db"

# Note: You can also update these values directly in database.py if preferred
